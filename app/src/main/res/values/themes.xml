<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Only used for the window between process start and the first Compose frame.
      All real theming lives in AtriumTheme.
    -->
    <style name="Theme.Atrium.Starting" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/starting_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
